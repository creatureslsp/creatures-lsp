import { libs } from "caos-util";
import Commands = libs.Commands;
/**
 * Gets a commands' definition object for a given variant.
 * Object has command
 * @param variant
 */
export declare function getCommands(variant: string): Commands;
